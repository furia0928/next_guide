import Alert from '@/components/feedback/Modal/Alert';

const ModalContainer = ({}) => {
  return (
    <div className="modal-container" id="modal-wrap">
      <Alert />
    </div>
  );
};

export default ModalContainer;
